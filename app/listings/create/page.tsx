import CreateForm from "./CreateForm";

export default function CreateListing() {
    return (
        <main>
            <h2 className="text-primary text-center">Add a New Listing</h2>
            <CreateForm />
        </main>
    )
}